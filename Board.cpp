#include "Board.hpp"

using namespace std;

void ariel::Board::post(unsigned int row,unsigned int column,ariel::Direction dir, string const &str){}



string ariel::Board::read(unsigned int row,unsigned int column,ariel::Direction dir,unsigned int length){
   /* checks_input(this->board,row,column,length);
    string answer;
    for(unsigned int i =0; i<length;i++){
        if(dir==ariel::Direction::Horizontal){
            answer.append(this::board[row][column+i]);
        }else{
            answer.append(this::board[row+i][column]);
        }
    }
    return answer;*/
    return "hi";
}
/*
void checks_input(ariel::Board->board,unsigned int row,unsigned int column,unsigned int length){
    if(length==0){  throw("dont troll me");    }
    if(row->board)
}

*/
